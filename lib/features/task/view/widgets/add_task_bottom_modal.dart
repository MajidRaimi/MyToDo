// create a bottom modal named AddTaskBottomModal
import 'package:flutter/material.dart';

class AddTaskBottomModal extends StatelessWidget {
  const AddTaskBottomModal({super.key});

  @override
  Widget build(BuildContext context) {
    // add bottom modal
    return Column(
      children: [
        Text('Add Task')
      ],
    );
  }
}
